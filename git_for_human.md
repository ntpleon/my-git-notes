# pwd = print working directory
# mkdir = make new directory
# rmdir = remove directory
# ls = list all file in directory
# cd = change directory
# git init = initialized (make this directory to start git)
# touch = create new file
# cat = concat
# git status
# git add = track the specific file --> "I'm watching you!" -- Staging area
# git commit -m "Add(V.1) new python file" = save snapshot this file (-m = message)
# git log = see history (how many commit?)
